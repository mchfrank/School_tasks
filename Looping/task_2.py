# Инициализация переменной для хранения суммы
summary = 0
# Цикл для ввода 10 целых чисел и их суммирования
for i in range(10):
    number = int(input())
    summary += number
# Вывод суммы
print(summary)
